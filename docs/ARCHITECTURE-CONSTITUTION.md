# 아키텍처 원칙 (ARCHITECTURE-CONSTITUTION.md)

1. **상태 분리**: 자동화 엔진의 상태는 Supabase DB를 통해 관리하며, 로컬 엔진은 Stateless함을 지향한다 (세션 제외).
2. **보안 우선**: 세션 정보(`session.json`, `.env`)는 절대 외부로 노출하지 않는다.
3. **휴먼 시뮬레이션**: 모든 자동화 동작은 실제 사람의 행동을 모방하여 탐지를 최소화한다.
